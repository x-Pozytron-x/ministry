Fix new file creation flow.

Problem:
When creating a new encrypted congregation file, validation fails because congregationName is required but there is no input field for it in the UI.

Tasks:
1. Add "Congregation Name" input field to the create new file screen
2. Make it required before password step completion OR part of same step
3. Ensure congregationName is included in root JSON structure
4. Validate both:
   - congregationName is not empty
   - password and confirm password match
5. Ensure error messages are shown inline in UI, not as generic crash

Do not remove password flow.
Do not change encryption logic.
Only fix missing UI + validation mismatch.